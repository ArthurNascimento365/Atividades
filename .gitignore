#include <stdio.h>
#include <stdlib.h>
int main()
{
    int ano;
    printf("\nDigite o ano que deseja verificar: ");
    scanf("%d",&ano);
    if ((ano%4==0) && (ano%100>0))
        printf("\n%d eh um ano bisexto!",ano);
    else
    	  printf("\n%d nao eh um ano bisexto!",ano);
}
