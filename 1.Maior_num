#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

main (){
	int num1,num2,aux;	
	printf("\nDigite o 1o numero: ");
	scanf("%d",&num1);
	printf("\nDigite o 2o numero: ");
	scanf("%d",&num2);
	if (num1>num2)
		printf("\nMaior:\t%d",num1);
	else
		if (num2>num1)
			printf("\nMaior:\t%d",num2);
		else
			printf("\nSao iguais!");
